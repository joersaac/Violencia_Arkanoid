

--UNIQUE hace que los nickName no se puedan repetir

CREATE TABLE PLAYER(
	NICKNAME VARCHAR(15) PRIMARY KEY UNIQUE NOT NULL
);


CREATE TABLE PUNTAJE(
	PUNTAJE_ID SERIAL PRIMARY KEY,
	PUNTAJE INTEGER,
	NICKNAME VARCHAR(15)
);


ALTER TABLE PUNTAJE
ADD CONSTRAINT nickname_fk
FOREIGN KEY(NICKNAME)
REFERENCES PLAYER(NICKNAME)




